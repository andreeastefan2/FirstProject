{
  "name": "node",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test":"npx mocha tests --timeout=10000",
    "test:report":"npx mocha tests --reporter mochawesome --reporter-options reportDir=results,reportFilename=actualresult --timeout=10000",
 "test_get": "npx mocha tests/get-test.js --timeout=10000"
  },
  "author": "Andreea",
  "license": "ISC",
  "devDependencies": {
    "@faker-js/faker": "^8.2.0",
    "mocha": "^10.2.0",
    "mochawesome": "^7.1.3",
    "pactum": "^3.5.1"
  }
}
